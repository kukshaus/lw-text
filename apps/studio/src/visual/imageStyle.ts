/** @deprecated Import from `./boxStyle` — re-exports for compatibility. */
export {
  type BoxSize,
  type BoxSize as ImageSize,
  parseBoxSize,
  parseBoxSize as parseImageSize,
  applyBoxSizeToStyle,
  applyBoxSizeToStyle as applyImageSizeToStyle,
  applyBoxSizeToStyle as mergeImageSize,
  IMAGE_SIZE_PRESETS,
  parsePx,
  toPx,
} from "./boxStyle";
